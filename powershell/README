# Profiles

现在主要使用的是 Microsoft.PowerShell_profile.ps1。相对比较简单，只支持了自己最基础和必要的扩展。

xProfile.ps1 和 xMicrosoft.PowerShell_profile.ps1 都是以前的历史存档。这两个都使用了 oh-my-posh。

# Scripts

SetEnv.ps1 是代码片段的保留。仅供参考用。

# Modules

自定义脚本集合的模块。后边都整理到模块里，比 ps1 脚本方式更方便使用和管理。
