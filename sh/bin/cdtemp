#! /bin/zsh

tmp_root="$1"
if [ -z $tmp_root ]; then
  tmp_root="$MY_TEMP_ROOT"
fi
if [ -z $tmp_root ]; then
  echo "No temp root dir config"
  exit 1
fi
cd $tmp_root
tmp=tmp-$(date | shasum | cut -c1-6)
mkdir $tmp
cd $tmp

